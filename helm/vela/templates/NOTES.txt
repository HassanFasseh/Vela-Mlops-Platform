Vela has been installed as release "{{ .Release.Name }}" in namespace "{{ include "vela.namespace" . }}".

Check rollout status:
  kubectl get pods -n {{ include "vela.namespace" . }} -w

{{- if .Values.backend.externalService.enabled }}

Backend external LoadBalancer:
  kubectl get svc backend-external -n {{ include "vela.namespace" . }}
{{- end }}

{{- if .Values.ingress.enabled }}

Dashboard: http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}/dashboard
{{- else }}

Once the backend-external LoadBalancer has an external IP, the dashboard is at:
  http://<EXTERNAL-IP>/dashboard
{{- end }}

{{- if or (not .Values.secrets.groqApiKey) (not .Values.secrets.geminiApiKey) }}

WARNING: no LLM API key set (secrets.groqApiKey / secrets.geminiApiKey) — drift
explanations will not be generated until at least one is configured.
{{- end }}
